// import filesystem functionality and call it and store it in simple function
const fs = require('fs'); //fs stands for file system

fs.writeFileSync('hello.txt', 'Hello from Node.js'); //path to the file including the file name